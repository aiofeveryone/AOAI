package com.aoai.chat.ui

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

/**
 * Stub receiver declared in AndroidManifest.
 * Add handling here if quick-thought / widget-related broadcasts are needed.
 */
class QuickThoughtReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context?, intent: Intent?) {
        // No-op: manifest reference only. Implement when required.
    }
}
